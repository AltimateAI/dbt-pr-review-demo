name: altimate_dbt_review_demo
version: "1.0.0"
config-version: 2

profile: altimate_dbt_review_demo

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  altimate_dbt_review_demo:
    staging:
      +materialized: view
    marts:
      +materialized: table

seeds:
  altimate_dbt_review_demo:
    +quote_columns: false
